# Frontend Environment Variables
# All variables must be prefixed with VITE_ to be exposed to the client

# Backend API URL
# For local dev: http://localhost:5000/api
# For Railway (two separate services): https://your-backend.railway.app/api
# For Railway (single service / monorepo): /api
VITE_API_URL=https://team-task-manager-backend.up.railway.app/api
